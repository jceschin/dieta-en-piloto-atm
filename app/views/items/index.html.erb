<!-- index.html.erb -->
<div class="container">
  <% @items.each do |item|  %>
    <%= render 'item-modal', item: item, order_item: @order_item %>
  <% end %>

  <div class="row">
    <div class="col-sm-12">
      <%= render 'cart-modal', order: @order%>
    </div>
  </div>

  <!--Btn Carousel-->

  <div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
    <div id="carousel-item-override" class="carousel-inner">
      <div id="carousel-item-override" class="carousel-item active">
        <div class="categories">
          <% @categories[1..4].each do |category| %>
          <%= link_to category_path(category.id), class: "category hvr-grow" do %>
            <%= image_tag "#{category.picture}", alt: "#{category.name}", id:"#{category.name}", class: "category hvr-grow-s" %>
              <h2> <%= category.name.upcase %> </h2>
            <% end %>
          <% end %>
        </div>
      </div>

      <div id="carousel-item-override" class="carousel-item">
        <div class="categories">
          <% @categories[5..8].each do |category| %>
          <%= link_to category_path(category.id), class: "category hvr-grow" do %>
            <%= image_tag "#{category.picture}", alt: "#{category.name}", id:"#{category.name}", class: "category hvr-grow-s" %>
              <h2> <%= category.name.upcase %> </h2>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
    <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
      <i style="font-size: 20px; color:green" class="fas fa-arrow-alt-circle-left"></i>
      <span class="sr-only">Previous</span>
    </a>
    <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
      <i style="font-size: 20px; color:green" class="fas fa-arrow-alt-circle-right"></i>
      <span class="sr-only">Next</span>
    </a>
 </div>


  <!-- -->

  <div class="text-center pt-3 pb-0 sugerencias bg-white">
    <h2>Tus sugeridos</h2>
  </div>
  <div class="item-cards">
    <% if @items.count == 0 %>
      <%= render 'control_limit' %>
    <% else %>
      <% @items.each do |item|  %>
        <%= render 'item-card', item: item %>
      <% end %>
    <% end %>
  </div>
  <div class="filler-space-consumption"></div>
  <div class="add-consumption fixed-bottom">
    <%= link_to new_item_path, class: "hvr-grow-s d-flex justify-content-center" do%>
      <div class="btn-add-consumption d-flex align-items-center">
        <%= image_tag "components/plus_button_big.png" %>
        <h2>Registrar consumo</h2>
      </div>
    <% end %>
  </div>
</div>

<script>
  const ensaladas = document.querySelector('#ensaladas');
    console.log(ensaladas);
  const hamburguesas = document.querySelector('#hamburguesas');
    console.log(hamburguesas);
  const pollos = document.querySelector('#pollos');
    pollos.addEventListener('click', (event) => {
      console.log(event.currentTarget)
    });
  const carnes = document.querySelector('#carnes');
    console.log(carnes);
  const postres = document.querySelector('#postres');
    console.log(postres);
  const pizzas = document.querySelector('#pizzas');
    console.log(pizzas);
  const pastas = document.querySelector('#pastas');
    console.log(pastas);
  const pescados = document.querySelector('#pescados');
    console.log(pescados);
</script>
